<div class="container p-4">
    <div class="row">
        <div class="card text-center">
            <div class="card-body">
                <h3>Welcome {{user.fullname}}</h3>
                <h4>{{user.username}}</h4>
                <a href="./links/add" class="btn btn-primary m-4">
                    Save your Links
                </a>
            </div>
        </div>
    </div>
</div>